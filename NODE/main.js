//const blogs= require("./my-own");
//Destructure
const { generateDescription } = require("./my-own");

const result = generateDescription("Raktim is a teacher at Broadway");
console.log(result);

//Create a node module for calculating time
// area of cuboid
// Import it to main.js file
